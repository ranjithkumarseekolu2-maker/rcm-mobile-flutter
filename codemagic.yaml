workflows:
  android-debug:
    name: Android Debug Build
    max_build_duration: 60
    environment:
      flutter: 3.24.5
    scripts:
      - name: Create local.properties
        script: |
          set -e
          echo "flutter.sdk=$FLUTTER_ROOT" > "$CM_BUILD_DIR/android/local.properties"
          echo "flutter.versionName=1.0.0" >> "$CM_BUILD_DIR/android/local.properties"
          echo "flutter.versionCode=1" >> "$CM_BUILD_DIR/android/local.properties"
          echo "sdk.dir=$ANDROID_SDK_ROOT" >> "$CM_BUILD_DIR/android/local.properties"
          echo "=== FLUTTER_ROOT is: $FLUTTER_ROOT ==="
          echo "=== ANDROID_SDK_ROOT is: $ANDROID_SDK_ROOT ==="
          echo "=== local.properties contents: ==="
          cat "$CM_BUILD_DIR/android/local.properties"
          echo "=== Flutter gradle tools exist: ==="
          ls "$FLUTTER_ROOT/packages/flutter_tools/gradle/"
      - name: Write google-services.json
        script: |
          cat > "$CM_BUILD_DIR/android/app/google-services.json" << 'EOF'
          {
            "project_info": {
              "project_number": "338627372093",
              "project_id": "realtorworks-89f65",
              "storage_bucket": "realtorworks-89f65.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:338627372093:android:ae7d3b9c6d6efa89d57654",
                  "android_client_info": {
                    "package_name": "com.realtorworks.agent"
                  }
                },
                "oauth_client": [],
                "api_key": [
                  {
                    "current_key": "AIzaSyA8OXRuLAGDZYBwC3pv2vDrN5BPBIn80Ic"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              }
            ],
            "configuration_version": "1"
          }
          EOF
      - name: Check Kotlin files
        script: |
          echo "=== MainActivity.kt ==="
          cat "$CM_BUILD_DIR/android/app/src/main/kotlin/com/realtorworks/agent/MainActivity.kt"
          echo "=== MainApplication.kt ==="
          cat "$CM_BUILD_DIR/android/app/src/main/kotlin/com/realtorworks/agent/MainApplication.kt"
          echo "=== settings.gradle ==="
          cat "$CM_BUILD_DIR/android/settings.gradle"
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build debug APK
        script: |
          set -e
          cd android
          chmod +x gradlew
          ./gradlew clean assembleDebug 2>&1
    artifacts:
      - build/app/outputs/apk/debug/*.apk
    publishing:
      email:
        recipients:
          - ranjithkumarseekolu2@gmail.com
        notify:
          success: true
          failure: true
```

Commit this, rebuild, then paste the output of **"Create local.properties"** step specifically. I need to see:
```
=== FLUTTER_ROOT is: /path/to/flutter ===
=== ANDROID_SDK_ROOT is: /path/to/android/sdk ===
=== local.properties contents: ===
flutter.sdk=/path/to/flutter
...
=== Flutter gradle tools exist: ===
(list of files)
